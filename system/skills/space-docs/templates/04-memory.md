<!-- Template: measured runtime footprint per component/building block. Measure it
     (ps RSS, docker stats, etc.); state idle vs under load; note how to reproduce. -->
# Memory footprint — <Space name>

Measured on <machine/context>, <mode>, idle. Approximate RSS.

| Component | Building blocks | Memory (idle) | Notes |
|---|---|---|---|
| <name> | <blocks> | <MB> | <note> |

## The headline
<the dominant cost + the cheap alternative, in one or two lines>

## How to reproduce
```bash
<commands used to measure>
```
