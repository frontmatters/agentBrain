---
date: 2026-05-31
type: skill
tags: [skill, config, agentbrain]
status: active
id: cdd6de05-e2d3-57d4-908a-f68176a408b6
---

# config

Inspect and modify agentBrain configuration post-onboarding.

Focus-based skill — walk-all (default) or one scope (`locale`, `addons`, `hooks`, `preferences`, `shell-rc`). Read-mostly: shows current state and routes mutations to the right tool rather than editing config directly.

See `SKILL.md` for the per-scope behaviors.
