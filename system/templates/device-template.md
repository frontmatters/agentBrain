---
date: 2026-08-08
type: reference
tags: [template, devices, reference]
id: 94ff8688-9ac9-56f1-ae9c-2383b8c4ca59
---

# HOSTNAME

<!-- Canonical rule: the HOSTNAME is canonical for all wikilinks
     ([[hostname]]). Use a `display:` frontmatter field only when the
     human-facing name differs; never link via the display name. -->

Display: <human-friendly name, if different>

## Role

<!-- One line: what is this device for? (primary dev laptop, NAS backup
     target, services host, music sandbox, ...) -->

## Specs

- Model:
- CPU / RAM / Storage:
- OS:

## Network

<!-- Reachability by NAME only: hostname, tailnet name, .local. NEVER store
     passwords, keys, or tokens here — reference the helper or keychain item
     by name (see local/integrations/ and local/security/). -->

- Hostname:
- Reachable via:

## Services

<!-- What runs here that agents should know about (git server, exit node,
     backups, schedulers). Link the matching [[integration]] note. -->

## Status & lifecycle

<!-- active / superseded / eol-incoming — and by what. Update
     `last-confirmed` in the frontmatter when you verify this note. -->

## Related

- [[index]] — the devices hop-page (one line per device, link back here)
